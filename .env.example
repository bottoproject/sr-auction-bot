# Supabase Configuration
# Note: Variables with SUPABASE_ prefix are reserved by Supabase and cannot be set as secrets.
# SUPABASE_URL and SUPABASE_KEY (or SUPABASE_SERVICE_ROLE_KEY) are automatically available in Edge Functions.
# These are needed for local development but will NOT be uploaded as secrets by the setup-secrets.js script.
SUPABASE_URL=your_supabase_url
SUPABASE_KEY=your_supabase_key  # Service role key for admin access
SUPABASE_DB_PASSWORD=your_database_password
SUPABASE_PROJECT_ID=your_project_id

# Discord Configuration
# These need to be set as secrets in Supabase
DISCORD_BOT_TOKEN=your_discord_bot_token
DISCORD_APPLICATION_ID=your_discord_application_id
DISCORD_PUBLIC_KEY=your_discord_public_key
DISCORD_CHANNEL_ID=your_discord_channel_id
DISCORD_WEBHOOK_URL=your_discord_webhook_url  # Optional if using bot token

# SuperRare Configuration
# These need to be set as secrets in Supabase
SUPERRARE_API_URL=https://api.superrare.com/graphql
BOTTO_CONTRACT_ADDRESS=0x66cd3ede22a25eef3cc8799381b99b1d4f0983f8

# Application Configuration
# These need to be set as secrets in Supabase
POLLING_INTERVAL_MS=60000  # 1 minute in milliseconds 